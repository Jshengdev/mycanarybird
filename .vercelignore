# Exclude from Vercel CLI uploads. Only the Next.js landing at
# canaryT/canary-landing needs to ship.

# Node + build outputs at every depth
**/node_modules
**/.next
**/dist
**/dist-ssr
**/out

# Root-level legacy Vite app — anchored with leading slash so we don't
# accidentally match files deep inside canaryT/canary-landing/.
/src
/public
/vite.config.js
/eslint.config.js
/index.html
/README.md
/canary-dashboard

# Nested experiment / legacy trees the current prod doesn't need
canaryT/canary-prototyping
canaryT/canary-dashboard
canaryT/OLD canary-landing
canaryT/docs
canaryT/.git
canaryT/.gitignore

# Editor
.DS_Store
*.log
